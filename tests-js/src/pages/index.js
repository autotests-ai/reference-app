const { LoginPage } = require('./login.page');
const { RegisterPage } = require('./register.page');
const { HomePage } = require('./home.page');
const { HeaderPage } = require('./header.page');

module.exports = { LoginPage, RegisterPage, HomePage, HeaderPage };
