# Selenide WebDriver is thread-local — JUnit parallel (even parallelism=1) rotates worker
# threads and flakes remote sessions on prod-pyramid. Smoke pyramid is small; run sequential.
junit.jupiter.execution.parallel.enabled=false
